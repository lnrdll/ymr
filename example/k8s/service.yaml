---
apiVersion: v1
kind: Service
metadata:
  name: app-service # from-param: {{ .name }}-svc
  labels:
    app: nginx
spec:
  selector:
    app: nginx
  type: NodePort
  ports:
    - port: 80
      targetPort: 80 # from-param: {{ .targetPort }}
      nodePort: 30008
